name: jarvis_app
description: "J.A.R.V.I.S. — Asistente Virtual de Voz Multi-Tenant SaaS"

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  record: ^5.1.2
  path_provider: ^2.1.2
  http: ^1.2.0
  audioplayers: ^6.0.0
  fl_chart: ^0.66.0
  google_fonts: ^6.2.1
  shared_preferences: ^2.2.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
